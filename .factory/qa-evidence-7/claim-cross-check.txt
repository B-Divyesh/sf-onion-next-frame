Claim tags:
tests/claims.spec.ts:6:test('@claim:demo-sandbox sample demo is ready and does not save', async ({ page }) => {
tests/claims.spec.ts:20:test('@claim:sample-six-frame-demo loads the promised six-frame run cycle', async ({ page }) => {
tests/claims.spec.ts:27:test('@claim:sequence-import imports numbered PNG files and an animated GIF', async ({ page }) => {
tests/claims.spec.ts:61:test('@claim:three-layer-preview shows and adjusts three neighbour layers', async ({ page }) => {
tests/claims.spec.ts:75:test('@claim:contact-sheet exports every frame as one PNG', async ({ page }) => {
tests/claims.spec.ts:90:test('@claim:project-transfer exports and imports a project JSON file', async ({ page }) => {
tests/claims.spec.ts:152:test('@claim:privacy-local keeps artwork requests on the same origin', async ({ page }) => {
tests/claims.spec.ts:168:test('@claim:offline-reload works offline after the first visit', async ({ page, context }) => {
tests/claims.spec.ts:180:test('@claim:local-restore restores the latest real sequence after reload', async ({ page }) => {
tests/claims.spec.ts:215:test('@claim:free-use has no payment or account gate', async ({ page }) => {

Visible interaction promises:
src/app.ts:40:    ${demo ? `<aside class="demo-banner" aria-label="Demo status"><span><strong>Demo</strong> — sample data, nothing is saved</span><div><button class="text-button" id="reset-demo" type="button">Reset demo</button><a href="/" data-nav>Start for real</a></div></aside>` : ''}
src/app.ts:136:          <div class="drop-cue" aria-hidden="true">Drop PNG or GIF files</div>
src/app.ts:156:          <button class="key key-primary" id="export-sheet" type="button" disabled>Export contact sheet <kbd>E</kbd></button>
src/app.ts:160:        <p class="keyboard-note">Keyboard: ← → changes frames. Shift jumps to an end.</p>
README.md:23:Open `/?demo=1` or <https://onion-next-frame.sociobot.in/?demo=1>. Six original sample frames load immediately. The cyan banner remains visible while demo mode is active. **Reset demo** restores the sample. **Start for real** discards it and returns to an empty or previously saved project. `/demo` remains available as a short link.
.factory/demo.md:6:- Leave: choose **Start for real**. The demo frames are discarded.

Tests referencing those interactions:
