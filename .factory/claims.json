[
  {
    "id": "demo-sandbox",
    "claim": "The sample demo saves nothing and does not read the real project",
    "where": "demo banner, landing limits section, privacy page, README, demo documentation",
    "test": "npm test -- --grep @claim:demo-sandbox",
    "sandbox": "fresh browser context at /?demo=1; assert the banner, no app IndexedDB, and reset restores defaults"
  },
  {
    "id": "sample-six-frame-demo",
    "claim": "The sample demo loads a six-frame run cycle",
    "where": "landing hero, README",
    "test": "npm test -- --grep @claim:sample-six-frame-demo",
    "sandbox": "fresh browser context at /?demo=1; assert the frame counter, six frame controls, and loaded status"
  },
  {
    "id": "demo-first-viewport",
    "claim": "The demo opens with frame 03, its comparison canvas, and frame controls visible without scrolling",
    "where": "demo route, demo documentation",
    "test": "npm test -- --grep @claim:demo-first-viewport",
    "sandbox": "fresh browser contexts at 390×844 and 1440×900; assert the seeded canvas, frame counter, and previous/next controls are inside each viewport"
  },
  {
    "id": "sequence-import",
    "claim": "Imports numbered PNG files in natural filename order and animated GIF frames",
    "where": "landing light-table introduction, tool empty state, how it works, README",
    "test": "npm test -- --grep @claim:sequence-import",
    "sandbox": "fresh browser context; import out-of-order PNG fixtures and a two-frame GIF fixture"
  },
  {
    "id": "scope-boundaries",
    "claim": "Does not include painting, frame generation, accounts, collaboration, or sync",
    "where": "landing limits section, README, terms page",
    "test": "npm test -- --grep @claim:scope-boundaries",
    "sandbox": "fresh demo context; assert the shipped interface has only local frame-review controls, no editable painting surface, account fields, collaboration controls, or remote-sync requests"
  },
  {
    "id": "drag-drop-import",
    "claim": "Drops PNG or GIF files into the preview to import them",
    "where": "preview drop cue and researched brief",
    "test": "npm test -- --grep @claim:drag-drop-import",
    "sandbox": "fresh browser context; dispatch a real PNG File through the preview drop target and assert it becomes the loaded sequence"
  },
  {
    "id": "three-layer-preview",
    "claim": "Shows previous, current, and next frames with independent layer controls",
    "where": "landing hero, viewer, how it works, README",
    "test": "npm test -- --grep @claim:three-layer-preview",
    "sandbox": "demo data; assert three controls, neighbour labels, and changed canvas pixels after an opacity adjustment"
  },
  {
    "id": "contact-sheet",
    "claim": "Exports every source frame in one PNG contact sheet",
    "where": "export button, how it works, README",
    "test": "npm test -- --grep @claim:contact-sheet",
    "sandbox": "demo data; download export, verify PNG signature, size, name, and six-frame completion status"
  },
  {
    "id": "keyboard-shortcuts",
    "claim": "Left and right arrows change frames, Shift jumps to either end, and E exports the contact sheet",
    "where": "layer key labels, export key label, keyboard note, design notes",
    "test": "npm test -- --grep @claim:keyboard-shortcuts",
    "sandbox": "demo data with focus outside controls; exercise both Shift-arrow endpoints and E, then verify the contact-sheet download"
  },
  {
    "id": "project-transfer",
    "claim": "Exports and imports a portable project JSON file",
    "where": "project controls, README",
    "test": "npm test -- --grep @claim:project-transfer",
    "sandbox": "demo data; export JSON, validate its format and six embedded frames, then import it back into the demo"
  },
  {
    "id": "privacy-local",
    "claim": "Images stay on the device with no account, upload, analytics, or advertising",
    "where": "landing facts, privacy page, README",
    "test": "npm test -- --grep @claim:privacy-local",
    "sandbox": "capture the complete demo flow network log; assert every request is same-origin and no account or embedded ad controls exist"
  },
  {
    "id": "offline-reload",
    "claim": "Works offline after the first visit",
    "where": "landing facts, header status, privacy page, offline fallback, README",
    "test": "npm test -- --grep @claim:offline-reload",
    "sandbox": "fresh browser contexts at /demo; assert Online before a blocked registration, Ready offline after service-worker readiness, then reload under offline mode and assert the sample viewer works"
  },
  {
    "id": "local-restore",
    "claim": "Restores the latest real sequence after a reload",
    "where": "landing limits section, privacy page, README",
    "test": "npm test -- --grep @claim:local-restore",
    "sandbox": "fresh browser context; import two PNG files, wait for IndexedDB, reload, and assert both frames return"
  },
  {
    "id": "start-for-real",
    "claim": "Start for real discards the demo and returns to the empty or previously saved real project",
    "where": "demo banner, README, demo documentation",
    "test": "npm test -- --grep @claim:start-for-real",
    "sandbox": "fresh browser context; save a real sequence, enter the demo, choose Start for real, and assert the saved real sequence returns instead of the sample"
  },
  {
    "id": "free-use",
    "claim": "Free to use",
    "where": "landing facts, terms, README",
    "test": "npm test -- --grep @claim:free-use",
    "sandbox": "fresh browser context; assert the free-use fact and absence of account, buy, checkout, or login gates"
  }
]
